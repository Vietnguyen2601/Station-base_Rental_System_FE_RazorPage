@page
@model EVStationRental.Presentation.Pages.Wallet.TopupModel
@{
    ViewData["Title"] = "Nạp ví";
}

<partial name="~/Pages/Shared/_Alerts.cshtml" />

<div class="page-card">
    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card-panel h-100">
                <p class="text-muted mb-1">Số dư hiện tại</p>
                <p class="display-6 text-success">@Model.CurrentBalance.ToString("n0") ₫</p>
                <a class="btn btn-link px-0" asp-page="/Wallet/History">Xem lịch sử ví</a>
            </div>
        </div>
        <div class="col-lg-8">
            <div class="card-panel">
                <h2 class="mb-2">Nạp tiền vào ví</h2>
                <p class="text-muted mb-4">
                    Chọn phương thức thanh toán phù hợp. Sau khi thanh toán thành công, hệ thống sẽ cập nhật số dư ví của bạn ngay lập tức.
                </p>
                <form method="post" class="form-modern">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="mb-3">
                        <label asp-for="Input.Amount" class="form-label"></label>
                        <div class="input-group">
                            <input asp-for="Input.Amount" class="form-control" />
                            <span class="input-group-text">VNĐ</span>
                        </div>
                        <div class="form-text">Tối thiểu 10.000 VNĐ, tối đa 50.000.000 VNĐ.</div>
                        <span asp-validation-for="Input.Amount" class="text-danger"></span>
                    </div>
                    
                    <div class="mb-4">
                        <label class="form-label fw-bold">Chọn phương thức thanh toán:</label>
                    </div>
                    
                    <div class="d-grid gap-2">
                        <button class="btn btn-success btn-lg" type="submit">
                            <i class="bi bi-credit-card"></i> Thanh toán qua PayOS
                        </button>
                        <button class="btn btn-outline-primary" type="submit" formaction="?handler=VNPay">
                            <i class="bi bi-wallet2"></i> Thanh toán qua VNPay
                        </button>
                        <button class="btn btn-outline-secondary" type="submit" formaction="?handler=Manual">
                            <i class="bi bi-lightning-fill"></i> Nạp nhanh (Thủ công)
                        </button>
                    </div>
                </form>
                <div class="mt-4">
                    <div class="alert alert-info">
                        <div class="small fw-bold mb-2"><i class="bi bi-info-circle"></i> Lưu ý:</div>
                        <ul class="small mb-0">
                            <li><strong>PayOS (Khuyến nghị):</strong> Cổng thanh toán nhanh chóng, an toàn với nhiều phương thức.</li>
                            <li><strong>VNPay:</strong> Hỗ trợ thanh toán qua thẻ ngân hàng và ví điện tử.</li>
                            <li><strong>Nạp nhanh:</strong> Dùng cho mục đích thử nghiệm, không qua cổng thanh toán.</li>
                            <li>Đảm bảo trình duyệt không chặn popup khi chuyển đến cổng thanh toán.</li>
                            <li>Bạn có thể xem mọi giao dịch trong mục <a href="/Wallet/History" class="alert-link">"Lịch sử ví"</a>.</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="~/Pages/Shared/_ValidationScriptsPartial.cshtml" />
}
